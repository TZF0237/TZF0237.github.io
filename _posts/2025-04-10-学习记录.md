---
title: "2025年4月10日学习记录"
categories:
  - algorithms
tags:
  - algorithms
date: 2025-04-10
read_time: true
comments: true
share: true
related: true
toc: true
description: "GitHub Pages渲染BUG记录 + C++对拍模板 + 常数优化技巧（读写优化、缓存优化、编译优化、算法常数优化）。"
---

## 重大BUG+对拍模板+常数优化

某些单词 GitHub 会报错 + 图片博客网页加载不了。

<!-- more -->

代码为以下这部分，防止报错每个以 `.` 隔开。报错原因在于双重的 `{}`，通过这种方式设置二维数组也会报错。

```
\.g.r.a.p.h.i.c.s.p.a.t.h.{.{.f.i.g./.}.}.
\.g.r.a.p.h.i.c.s.p.a.t.h.{.{.f.i.g.2./.}.}.
```

第二个 BUG 为图片在网站加载不了，在 GitHub 可以正常加载，如果必须看图片可以去 GitHub 仓库里看博客。

### C++对拍模板

比赛时不知道自己的代码能否过全部的测试点，或者不清楚哪一种情况错误，不妨试试**对拍**。

对拍有两个重要的点：构造数据和两种代码对比。

- **构造数据**：包括输入和输出数据。如果输入情况复杂，不能方便手工构造，而要编一个小程序随机生成。
- **两种代码**：低效率代码（暴力法实现，用作基准）和高效率代码（正解），对比输出即可知道错误。

#### 随机数基础

- `srand(x)`：产生以 x 为种子的随机数列
- `time(0)`：返回当前时间
- `rand()`：返回 $$[0,RAND\_MAX]$$ 内的一个随机正整数
- 生成 `[a,b]` 范围的随机数：`x=rand()%(b-a+1)+a;`
- 需要 $$[-n,n]$$ 内的随机数：`int x=rand()%(2*n-1)-n;`

#### 示例代码

```c++
#include<bits/stdc++.h>
using namespace std;
int x;
int bao(){/*暴力代码*/ return 0;}
int solve(){/*你的无敌代码*/ return 1;}
int main(){
    srand(time(0));
    while(1){
        x=rand();
        int ans1=bao(),ans2=solve();
        if(ans1!=ans2){
            printf("x=%d,ans1=%d,ans2=%d\n",x,ans1,ans2);
            return 0;
        }
    }
    return 0;
}
```

### 常数优化

> 过早的优化是万恶之源

#### 读写优化

1. 换行不要使用 `endl`，使用 `\n`
2. 如果使用 `cin/cout`，可以关闭流同步：`ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);`，但不能再与 `scanf/printf` 混用
3. 手写快读：

```c++
int read(){
    int s=0,m=0;char ch=getchar();
    while(!isdigit(ch)){if(ch=='-')m=1;ch=getchar();}
    while( isdigit(ch))s=(s<<3)+(s<<1)+(ch^48),ch=getchar();
    return m?-s:s;
}
```

#### 缓存优化

对于图的存储，使用结构体性能一般优于多个数组。

#### 编译优化

- 不要使用 register
- 多使用局部变量

#### 算法常数优化

- 对于整数而言，加减法、位运算最快，乘法次之，除法和取模最慢
- 如果对特定的数进行取模或乘除，可以加上 const 修饰符
- 考虑减少取模次数，或者用位运算替代取模
